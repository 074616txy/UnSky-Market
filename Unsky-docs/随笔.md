第一个问题：为什么在接口实现类和控制类要写这个private final StudentCertMapper studentCertMapper;而不是@Autowired private StudentCertMapper studentCertMapper;？是不是暂时只是测试http请求是否正确发送，前端与后端能否正确连通？第二个问题：关于启动类扫描@MapperScan("com.Market.**.mapper")必须是**吗，为什么不是*

# 第一个问题：`private final + 构造器` vs `@Autowired`

# 第二个问题：`@MapperScan("com.Market.**.mapper")`



二、学生认证数据建模 ✅

三、学生认证业务实现（🔥新增）
    3.1 提交认证申请
    3.2 查询认证状态
    3.3 管理员审核
    3.4 权限控制（未认证不能发布）

四、接口联调与验证（可简单写一段总结，不用细写）

五、今日成果总结
六、下一步任务
七、踩坑记录

---

核心业务约束（非常重要）：

1️⃣ 一个用户只能有一条认证记录（或只能提交一次）
- 防止重复提交认证申请
- 后续需要在 ServiceImpl 中做校验：
  查询 student_cert 是否已存在当前 userId 的记录

2️⃣ 认证状态初始值设计
- 提交时 status = 0（待审核）
- 后续由管理员修改为 1（通过）或 2（拒绝）

3️⃣ 用户表联动更新（关键设计）
- 提交认证申请后：
  user.auth_status = 2（审核中）

1. 防止重复提交（userId唯一）
2. 认证状态初始化为待审核
3. 强制绑定当前用户，不能信任前端传的 userId
4. 提交后同步更新 user 表认证状态

## 四
在完成学生认证模块的基础结构与接口声明之后，通过浏览器与 Apifox 对接口进行了初步联调测试。

本阶段的核心目标不是完善业务逻辑，而是验证以下链路是否打通：

- Controller 是否能够正确接收请求
- Service 是否能够被正常调用
- Mapper 是否能够正确访问数据库
- student_cert 表是否能够完成基础读写操作

测试结果表明，认证模块的基础调用链路（Controller → Service → Mapper → DB）已经成功建立，为后续提交认证申请、状态查询及管理员审核等业务实现提供了稳定基础。

👉 说明：认证表和用户表是“强关联”的，不是独立存在


==一、登录验证与受保护接口 ✔（你写得很好）==

==二、学生认证数据建模 ✔==

==三、学生认证业务实现（🔥重点）==
    ==3.1 提交认证申请==
        ==- 接口定义==
        ==- 核心理解==
        ==- 🔥业务设计关键点（放这里）==
        
    ==3.2 查询认证状态==
    ==3.3 管理员审核==
    ==3.4 权限控制==

==四、接口联调与链路验证（简洁收口）==

==五、今日成果总结==
==六、下一步任务==
七、踩坑记录
---

### 3.2 查询认证状态接口

#### 3.2.1 接口目标

- 用户查看自己的认证审核进度

#### 3.2.2 实操步骤

- 根据当前登录用户 id 查询自己的认证记录
- 返回状态、备注等信息

#### 3.2.3 核心代码

java

#### 3.2.4 核心知识点

- 为什么查询状态不能查别人的记录
- `remark` 字段的作用是什么

#### 3.2.5 测试结果

- 待审核
- 审核通过
- 审核拒绝

---

### 3.3 管理员审核接口

#### 3.3.1 接口目标

- 管理员对用户认证申请进行通过 / 拒绝处理

#### 3.3.2 实操步骤

- 根据申请 id 查询认证记录
- 更新 `student_cert.status`
- 同步更新 `user.authStatus`
- 拒绝时写入 `remark`

#### 3.3.3 核心代码

java

#### 3.3.4 核心知识点

- 为什么审核通过后要同步更新用户状态
- 为什么管理员审核是认证闭环的关键一步

#### 3.3.5 测试结果

- 审核通过
- 审核拒绝
- 用户再次查询状态变化成功

### 3.4 小结

- 已完成“申请提交 → 状态查询 → 管理员审核”主链路

---

## 四、将认证状态接入商品发布权限

### 4.1 为什么要做发布权限校验

- 学生认证的最终目的不是停留在表里
- 而是限制未认证用户不能发布商品

### 4.2 实操步骤

- 找到商品发布接口
- 在发布前查询当前用户认证状态
- 若未认证则直接返回错误
- 已认证则允许继续发布

### 4.3 核心代码

java

### 4.4 核心知识点

- 认证功能为什么必须和业务权限挂钩
- 为什么这里要校验 `authStatus`

### 4.5 测试结果

- 未认证用户发布失败：请先完成学生认证
- 已认证用户可正常发布

### 4.6 小结

- 学生认证正式转化为业务权限控制能力